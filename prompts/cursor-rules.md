# Cursor Rules: Shader Skills

Apply these rules to shader work in this repository.

- Read `prompts/universal-system-prompt.md` before writing shader code.
- Read only relevant references from `skills/codex/shader-art-coding/references/`.
- Preserve Shadertoy-style `mainImage`.
- Use `iDebugMode` for inspectable intermediate fields.
- Use formal mathematical names for reusable helpers.
- Do not introduce texture dependencies unless requested.
