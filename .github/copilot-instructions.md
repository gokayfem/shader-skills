# Copilot Instructions

This repo is a procedural shader art instruction pack.

- Use `prompts/universal-system-prompt.md` for global shader behavior.
- Use `skills/codex/shader-art-coding/references/` for formulas and GLSL snippets.
- Use `iDebugMode` branches for formula verification.
- Prefer formal mathematical naming for reusable helpers.
